# Copy this file to .env and EDIT it
UID=1000
GID=1000
COMPOSE_PROJECT_NAME=pbsk
# needed only if you're exposing through traefik
HTTP_HOSTNAME=pbskjd.office.spinspire.com
# uncomment to enable auditlog
# AUDITLOG=posts,users
# uncomment to use custom pocketbase release
# RELEASE=custom

# Test superuser credentials (upserted on startup via entrypoint.sh)
# Generate a random password:
#   openssl rand -base64 16
#   openssl rand -hex 16
#   pwgen -1 16
PB_SUPERUSER_EMAIL=admin@example.com
PB_SUPERUSER_PASSWORD=changeme123

# Test user credentials (created on startup via entrypoint.sh)
PB_USER_EMAIL=user@example.com
PB_USER_PASSWORD=changeme123
